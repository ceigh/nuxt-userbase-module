{
  "name": "nuxt-userbase",
  "version": "0.1.0",
  "description": "Userbase plugin for Nuxt",
  "author": "Artjom Löbsack <ceigh@pm.me> (http://ceigh.github.io)",
  "keywords": [
    "userbase",
    "nuxt"
  ],
  "main": "lib/module.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "nuxt test/fixture",
    "test": "NODE_ENV=test jest",
    "format": "prettier --write --ignore-path .gitignore './**/*.{ts,json,vue}' '!.yarn/**'"
  },
  "devDependencies": {
    "@nuxt/types": "^0.7.6",
    "@types/jest": "^25.2.3",
    "@types/node": "^14.0.4",
    "jest": "^26.0.1",
    "nuxt": "^2.12.2",
    "prettier": "^2.0.5",
    "ts-jest": "^26.0.0",
    "typescript": "^3.9.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "prettier": {
    "trailingComma": "none",
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true
  }
}
