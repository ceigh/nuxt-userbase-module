{
  "name": "nuxt-userbase",
  "version": "0.1.0",
  "description": "Userbase plugin for Nuxt",
  "author": "Artjom Löbsack <ceigh@pm.me> (http://ceigh.github.io)",
  "private": true,
  "keywords": [
    "userbase",
    "nuxt"
  ],
  "main": "lib/module.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "NODE_ENV=test jest",
    "format": "prettier --write --ignore-path .gitignore './**/*.{ts,json,vue}' '!.yarn/**'"
  },
  "devDependencies": {
    "@nuxt/types": "^0.7.6",
    "@types/jest": "^25.2.3",
    "@types/node": "^14.0.4",
    "husky": "^4.2.5",
    "jest": "^26.0.1",
    "prettier": "^2.0.5",
    "prettier-config-standard": "^1.0.1",
    "ts-jest": "^26.0.0",
    "typescript": "^3.9.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "prettier": "prettier-config-standard",
  "husky": {
    "hooks": {
      "pre-commit": "yarn format && yarn test && yarn build"
    }
  },
  "dependencies": {
    "userbase-js": "^1.4.0"
  }
}
